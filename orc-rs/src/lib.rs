#![warn(missing_docs)]
mod common;
mod error;
mod datatype;
// mod orcfile;
// mod reader;
// mod writer;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
